@vlc-android/ 修改此项目 1.目前有 video audio browse ... tab 不再展示 browse  tab 2.确认目前语言设置是跟随系统? 还是默认 english

检查当前目录的 py 文件, 确认他们的设计, 我希望为 VLC 增加一个 tab 暂时用于调试, 以确认 "三 声道" 音频播放成功, 因此你需要一个方案将 py 功能复制到 VLC Android , 但是因为VLC 本身就具有强大的播放器能力, 设计方式可能不同, python 目前还需借助三方库, 双线程的设计依旧保留, 只需要一个 tab 来做现在  lcr\_player 咋在做的事情, 我需要你先阅读 py 和 VLC 确认设计方案, 我同意后才编码

--

你的设计不过关 , 我期望听到的应该是 LR 立体声 和 独立的 C 而不是相加全部堆到中上耳, 我想要的是 类似 三声道效果, 这就是为什么 C 使用了独线程 因为不希望他们相加, 你目前的设计一定是变为中上耳效果 而非我理想的声效 重新设计, 我的建议是尽量使用 VLC 本身实现, 比如 多线程示例进行不同维度控制? 但我其实不懂, 如果不行你可以考虑完全重新实现, 给出重新设计的方案

---

1.先只尝试  MediaCodec 最终无法实现再考虑集成 ffmpeg , 2.文件选择器即可,  3.将 python 原始功能都要复制过去, 比如 不同的模式 seprate leftonly.... 不同模式, 他们都有助于调试,你自己来决定 lcrtab 的ui 摆放. 开始实现吧

1.优化 UI 排版 现在无法确认是否成功选择文件, 2.优化逻辑 我记得之前 py是直接把 所有文件内容读到内存 有些耗费资源  如果目前也是这样你需要优化一下 3.添加 LCR tag 的日志 作用与  LCR 代码, 添加更多日志以方便调试如果有问题时快速确认,

---

需要你重新优化下内存使用分配逻辑, 目前逻辑如果大文件占用内容过多, 并且慢, 你先规划下方案, 我来确认

那就选择方案A , 设置上限值最大 200M 内存, 无论如何不允许超过它 包括多声道, 你可以预估好进行再这个范伟内最大化启动, , 其他的逻辑你自己考虑实现,

拖动后 app 会卡住 你需要优化那相关逻辑, 加载 音频的时间很长, 没有更优雅的方案吗

加载缓冲区如果大于指定大小的音频数据应该立即播放, 后方的音频缓存可以慢慢加载, 而不是所有加载后再播放, 目前的设计很笨重, 你需要优化一下

--

还有一点, 设计, 1.默认自动填充 left right 文件相同, 也就是假如我再 left 选了 文件, right 也使用同一文件即可, 这样使用相同文件无需过多使用内存,

1.UI 问题是字体没展示全 2.修复这个 停止播放后开始播放场景的 error 报错

1.修复假设 LR  在播放状态添加 Center 后未自动播放全体音效的场景, 你可以直接重置状态之后自动seek 到当前播放位置之类的操作, 再 检查下代码中是否有同类问题,  2.假设 LCR 同时播放会 有音频异常 Center 会有波段干扰之类异常, 比如 音频类似回音的效果, 但是 center only 不会出现, 请检查


1. SWITCH增加注释 (左右耳声道互换) 2.增加倍速播放功能, 支持常用的 1 1.25 1.5 1.75 2 等 btn 支持滑动控制, 范围为 1 -- 3 倍, 单独的一个 control block 放置他们  3. 支持循环播放, (默认) 自动的重新播放, 而不是播完后结束,
